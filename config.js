const config = {
  databaseHost: "localhost",
  databasePort: 3306,
  databaseUser: "root",
  databasePassword: "password",
  databaseDatabase: "arcader",

  serverPort: 80,
};

module.exports.config = config;
